name: sw_air_link
description: App simples para conectar o telefone ao navegador via SW Air Link.
publish_to: 'none'
version: 0.3.6+36

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
