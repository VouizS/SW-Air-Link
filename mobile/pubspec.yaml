name: sw_air_link
description: App simples para conectar o telefone ao navegador pelo SW Air Link.
publish_to: 'none'
version: 0.2.1+21

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
