name: sw_air_link
description: "SW Air Link: conexão simples entre Android e navegador."
publish_to: 'none'
version: 0.1.5+5

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
